# 打包前检查清单

## ✅ 已完成的配置

### 1. 图片生成配置
- ✅ `IMAGE_PROVIDER` 已设置为 `z-image-turbo`
- ✅ `Z_IMAGE_TURBO_HOST` 已设置为 `10.221.80.199`
- ✅ `Z_IMAGE_TURBO_PORT` 已设置为 `8187`
- ✅ 代码中默认 host 已更新为 `10.221.80.199`

### 2. Docker 配置
- ✅ `docker-compose.yml` 已包含所有必要的环境变量
- ✅ 包括 `Z_IMAGE_TURBO_SCRIPT_PATH`、`Z_IMAGE_TURBO_HOST`、`Z_IMAGE_TURBO_PORT`

## ⚠️ 打包前需要确认的事项

### 1. 脚本路径配置
**问题**：`run_prompt.py` 脚本在 `pythondemo/` 目录，可能在容器外

**解决方案（三选一）**：

**方案 A：使用环境变量（推荐）**
- 在 `.env` 文件或 `docker-compose.yml` 中设置：
  ```bash
  Z_IMAGE_TURBO_SCRIPT_PATH=../pythondemo/run_prompt.py
  ```
- 或者在 `userConfig.json` 中添加（如果支持）：
  ```json
  "Z_IMAGE_TURBO_SCRIPT_PATH": "../pythondemo/run_prompt.py"
  ```

**方案 B：将脚本复制到容器内**
- 将 `pythondemo/run_prompt.py` 复制到 `no_node-presenton/scripts/run_prompt.py`
- 这样代码会自动找到脚本

**方案 C：使用 Docker volume 挂载**
- 在 `docker-compose.yml` 中添加 volume：
  ```yaml
  volumes:
    - ./pythondemo:/app/pythondemo
  ```
- 然后设置环境变量：
  ```bash
  Z_IMAGE_TURBO_SCRIPT_PATH=/app/pythondemo/run_prompt.py
  ```

### 2. 网络连通性
**确认**：Docker 容器能否访问 `10.221.80.199:8187`？

**检查方法**：
```bash
# 在容器内测试连接
docker exec -it <container_name> curl http://10.221.80.199:8187
```

### 3. Python 环境
**确认**：脚本使用的 Python 版本和依赖是否在容器内可用？

**当前配置**：
- 容器使用 `python:3.11-slim-bookworm`
- 脚本可能需要额外的 Python 包（如 `urllib`、`json` 等，这些通常已包含）

### 4. 环境变量传递
**确认**：如果使用 Docker Compose，环境变量是否正确传递？

**检查方法**：
```bash
# 查看容器内的环境变量
docker exec -it <container_name> env | grep Z_IMAGE_TURBO
```

## 📋 打包步骤

### 1. 构建 Docker 镜像
```bash
docker build -t presenton:latest .
```

### 2. 使用 Docker Compose（推荐）
```bash
# 确保 .env 文件存在并配置正确
docker-compose up -d production
```

### 3. 检查服务状态
```bash
# 查看日志
docker-compose logs -f

# 检查容器状态
docker-compose ps
```

## 🔍 测试验证

### 1. 测试图片生成
1. 访问应用：http://localhost:5000
2. 创建一个新的演示文稿
3. 检查后端日志，确认：
   - 脚本路径正确
   - 能连接到 ComfyUI 后端
   - 图片生成成功

### 2. 查看日志
```bash
# 查看 FastAPI 日志
docker-compose logs -f production | grep -i "image\|z-image-turbo"
```

## 🐛 常见问题

### 问题 1：找不到脚本
**错误**：`Z-Image-Turbo script not found`

**解决**：
- 检查 `Z_IMAGE_TURBO_SCRIPT_PATH` 环境变量
- 确认脚本路径在容器内可访问
- 查看日志中的路径检查信息

### 问题 2：连接被拒绝
**错误**：`Connection refused` 或 `cannot reach backend`

**解决**：
- 确认 ComfyUI 后端正在运行
- 检查 `Z_IMAGE_TURBO_HOST` 和 `Z_IMAGE_TURBO_PORT` 配置
- 确认网络连通性（容器能否访问后端服务器）

### 问题 3：Python 执行错误
**错误**：`python3: command not found` 或模块导入错误

**解决**：
- 确认容器内有 Python 3.11
- 检查脚本依赖是否满足
- 查看脚本的 shebang 行（`#!/usr/bin/env python3`）

## 📝 当前配置摘要

```json
{
  "IMAGE_PROVIDER": "z-image-turbo",
  "Z_IMAGE_TURBO_HOST": "10.221.80.199",
  "Z_IMAGE_TURBO_PORT": "8187"
}
```

**脚本查找顺序**：
1. 环境变量 `Z_IMAGE_TURBO_SCRIPT_PATH`
2. `scripts/run_prompt.py`
3. `../pythondemo/run_prompt.py`

## ✅ 打包就绪检查

- [ ] `userConfig.json` 中 `IMAGE_PROVIDER` 为 `z-image-turbo`
- [ ] `Z_IMAGE_TURBO_HOST` 和 `Z_IMAGE_TURBO_PORT` 已配置
- [ ] 脚本路径已配置（环境变量或复制到容器内）
- [ ] 网络连通性已验证
- [ ] Docker 镜像构建成功
- [ ] 服务启动正常
- [ ] 图片生成功能测试通过

---

**最后更新**：2025-12-15
**状态**：✅ 基本配置已完成，需要确认脚本路径配置

