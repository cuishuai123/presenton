# 服务器部署图片生成问题排查指南

## 🔍 常见问题检查清单

### 1. 脚本路径问题

**检查方法**：
```bash
# 进入容器
docker exec -it <container_name> sh

# 检查脚本是否存在
ls -la /app/scripts/run_prompt.py

# 检查脚本权限
chmod +x /app/scripts/run_prompt.py
```

**预期结果**：文件应该存在且可执行

### 2. Python 环境问题

**问题**：Alpine Linux 可能使用 `python` 而不是 `python3`

**检查方法**：
```bash
docker exec -it <container_name> sh

# 检查 Python 命令
which python
which python3
python --version
python3 --version
```

**解决方案**：如果只有 `python` 没有 `python3`，需要修改代码或创建符号链接

### 3. 网络连通性问题

**检查方法**：
```bash
# 在容器内测试连接
docker exec -it <container_name> sh

# 测试 ComfyUI 后端连接
curl -v http://10.221.80.199:8187
wget -O- http://10.221.80.199:8187

# 如果 curl/wget 不可用，使用 Python
python -c "import urllib.request; urllib.request.urlopen('http://10.221.80.199:8187')"
```

**预期结果**：应该能连接到后端

### 4. 环境变量问题

**检查方法**：
```bash
docker exec -it <container_name> env | grep -E "IMAGE_PROVIDER|Z_IMAGE_TURBO"
```

**预期输出**：
```
IMAGE_PROVIDER=z-image-turbo
Z_IMAGE_TURBO_HOST=10.221.80.199
Z_IMAGE_TURBO_PORT=8187
```

### 5. 脚本依赖问题

**检查方法**：
```bash
docker exec -it <container_name> sh

# 测试脚本是否能运行
cd /app/scripts
python run_prompt.py --help
# 或
python3 run_prompt.py --help
```

**预期结果**：应该显示帮助信息

### 6. 输出目录权限问题

**检查方法**：
```bash
docker exec -it <container_name> sh

# 检查输出目录
ls -la /app_data/images/
# 检查权限
chmod 777 /app_data/images/
```

## 🛠️ 快速诊断脚本

在容器内运行以下命令进行完整诊断：

```bash
docker exec -it <container_name> sh << 'EOF'
echo "=== 诊断开始 ==="

echo "1. 检查脚本文件"
ls -la /app/scripts/run_prompt.py || echo "❌ 脚本文件不存在"

echo "2. 检查 Python"
which python && python --version || echo "❌ python 不可用"
which python3 && python3 --version || echo "❌ python3 不可用"

echo "3. 检查网络连接"
python -c "import urllib.request; urllib.request.urlopen('http://10.221.80.199:8187'); print('✅ 网络连接正常')" 2>&1 || echo "❌ 无法连接到后端"

echo "4. 检查环境变量"
env | grep -E "IMAGE_PROVIDER|Z_IMAGE_TURBO" || echo "❌ 环境变量未设置"

echo "5. 检查输出目录"
ls -la /app_data/images/ || echo "❌ 输出目录不存在"

echo "6. 测试脚本执行"
cd /app/scripts
python run_prompt.py --help 2>&1 | head -5 || python3 run_prompt.py --help 2>&1 | head -5 || echo "❌ 脚本无法执行"

echo "=== 诊断结束 ==="
EOF
```

## 🔧 常见问题修复

### 问题 1：Python 命令不匹配

**症状**：日志显示 `python3: command not found`

**修复方法 A**：创建符号链接
```bash
docker exec -it <container_name> sh
ln -s $(which python) /usr/local/bin/python3
```

**修复方法 B**：修改代码使用 `python` 而不是 `python3`

### 问题 2：脚本权限不足

**修复方法**：
```bash
docker exec -it <container_name> chmod +x /app/scripts/run_prompt.py
```

### 问题 3：网络隔离

**症状**：`Connection refused` 或 `cannot reach backend`

**修复方法**：
- 检查 Docker 网络配置
- 确认 ComfyUI 后端在运行
- 检查防火墙规则
- 如果后端在同一服务器，尝试使用 `host.docker.internal` 或容器 IP

### 问题 4：环境变量未传递

**修复方法**：
- 检查 `docker-compose.yml` 中的环境变量配置
- 确认 `.env` 文件存在（如果使用）
- 检查 `userConfig.json` 配置

## 📋 完整排查步骤

### 步骤 1：查看实时日志
```bash
docker-compose logs -f production | grep -i "image\|z-image-turbo\|error"
```

### 步骤 2：手动测试脚本
```bash
docker exec -it <container_name> sh

cd /app/scripts
python run_prompt.py \
  --prompt "test image" \
  --host 10.221.80.199 \
  --port 8187 \
  --save-dir /app_data/images \
  --save-name test.png \
  --timeout 300
```

### 步骤 3：检查后端日志
查看 ComfyUI 后端的日志，确认是否收到请求

### 步骤 4：检查文件生成
```bash
docker exec -it <container_name> ls -la /app_data/images/
```

## 🎯 最可能的问题

根据经验，最常见的问题是：

1. **Python 命令不匹配**（70%）
   - Alpine 使用 `python` 而不是 `python3`
   - 解决方案：修改代码或创建符号链接

2. **网络连通性**（20%）
   - 容器无法访问后端服务器
   - 解决方案：检查网络配置和防火墙

3. **环境变量未传递**（10%）
   - Docker Compose 未正确传递环境变量
   - 解决方案：检查 docker-compose.yml 配置

## 📝 需要提供的信息

如果问题仍然存在，请提供：

1. **容器日志**：
   ```bash
   docker-compose logs production | grep -A 10 -B 10 "z-image-turbo\|image"
   ```

2. **诊断脚本输出**（运行上面的诊断脚本）

3. **手动测试结果**（运行上面的手动测试命令）

4. **环境信息**：
   - Docker 版本
   - 操作系统
   - 网络配置

---

**最后更新**：2025-12-15

